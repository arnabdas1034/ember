node_modules
out
release
dist
*.log
.DS_Store
.env

# Downloadable at build time via `npm run fetch:model` — too big for git
resources/models

# Server runtime data (accounts, encrypted keys, secret) — never commit
server/data
server/node_modules
